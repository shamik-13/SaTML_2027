# Claim ledger

Generated by `proto/t71_claim_ledger.py`. Values are recomputed from the artefacts on
every run, never transcribed. The point of the **Arm** column is the failure review 14
named: an experiment that is correct but whose paper sentence generalises it past the
window, order, seed or dataset it was actually run on.

| Claim | Value | Arm actually run | Assumption | Source script | Shown in | Body wording |
|---|---|---|---|---|---|---|
| Calibration required at LSPR23 flow scale, LOND/e-LOND, horizon-uniform | `3.27e+08` | algebraic; no window, order or seed | none beyond a finite evidence ceiling M and gamma_t = 1/T | `t21f_H6_scaling.py` | Fig. 2 | `$3.3\times10^{8}$` |
| Calibration units per hypothesis at k=1 (LOND/e-LOND, LORD++) | `20 / 40` | algebraic; k/c_0, dataset- and unit-independent | none beyond the cold-start coefficient c_0 | `closed form k/c_0` | Sec. III-B prose | `$20$ calibration units per hypothesis for LOND/e-LOND and $40$ for LORD++` |
| Alert blur at the two-hour headline unit, primary window | `16.1` | primary window 0.55, seed 0, FIRST-FLOW order (blur needs alerts at every width) | dataset labels (blur counts malicious atoms) | `t47_W7_coverage.py` | Fig. 3 (lower panel) | `$16.1$ at the two-hour headline unit` |
| Exact padding cost per alert, primary window | `23, 24, 33` | primary window 0.55, seed 0, CANONICAL order | labels (to know which episodes are true detections); NOT e-validity | `t28b_reallevel_padding.py` | Table I, Fig. 4A | `23, 24 and 33 added flows` |
| Exact padding cost, secondary window (median) | `6` | secondary window 0.62, seed 0, CANONICAL order; NOT replayed | labels; NOT e-validity | `t28b_reallevel_padding.py` | Table I, Fig. 4A | `median cost of six` |
| A single fixed multiplier c=10 defeats every canonical alert | `canonical@0.55: 3/3; canonical@0.85: 34/34` | LSPR23 only, canonical order, windows 0.55 and 0.85; state-free arms | attacker knows its own arity m (free on LSPR23: 674/674 detected episodes sit on host pairs with no benign traffic). Does NOT transfer to AIT, where the episode contains benign flows the attacker cannot count. | `t66_nonoracle_padding.py` | Sec. V-C prose, App. E | `on LSPR23, $c=10$` |
| Per-alert real-flow replay reproduces the closed form, primary window | `3/3 at the predicted 23--33 flows` | 0.55 and 0.62, seed 0, CANONICAL order; both windows replayed (14 alerts, all suppressed at the closed form) | labels; NOT e-validity | `t28b_reallevel_padding.py (replay arm)` | Table I, Sec. V-D | `all $3/3$ canonical primary-window detections` |
| AIT transfer: suppressible flow-only detections, canonical order | `78 of 79` | AIT-LDSv2.0, 8 held-out organisations, CANONICAL order, flow-only detector | labels; NOT e-validity, NOT A1 | `t67_ait_order.py` | Table I, Fig. 4B | `78 of 79` |
| Suppression cost under the max-min-optimal horizon-aware allocation | `105 true detections, median r*=2946, 0 FP` | LSPR23 primary 0.55, seed 0, CANONICAL order, gamma_t = 1/T (needs a known horizon) | labels; NOT e-validity. The control arm reproduces the shipped poly numbers. | `t73_uniform_padding.py` | Table III, Fig. 4A | `$2{,}946$` |
| Detection count under the two spending allocations, primary window | `3 -> 105 TRUE detections` | LSPR23 primary 0.55, seed 0, CANONICAL order; gamma poly -> gamma_t = 1/T is the ONLY thing that changes. Both cells have 0 false discoveries. | labels (a true detection needs one); NOT e-validity | `t73_uniform_padding.py` | Abstract, Table I, Sec. V-D | `detects $105$ of 275 malicious episodes at the primary window` |
| Per-alert real-flow replay under horizon-aware spending, both windows | `0.55: 105/105; 0.62: 107/107` | LSPR23 0.55 and 0.62, seed 0, CANONICAL order, gamma_t = 1/T, 200 draws. PER-ALERT at the level each alert received on the UNPERTURBED trajectory -- not a joint rerun of the controller under attack. Counts TRUE detections: the 0.62 cell also has one false discovery, which is not in the 107. | labels; NOT e-validity. Pads are real pool flows, so the e-values are measured; the aggregation and the level are arithmetic. | `t74_defended_replay.py` | Abstract, Sec. V-D | `suppresses $105/105$ and $107/107$ true detections` |
| Per-host-pair volume cap: alerts still suppressible UNDER the cap | `cap 100: 5/105 at 3.396% benign truncated; cap 300: 14/105 at 1.037% benign truncated` | LSPR23 primary 0.55, seed 0, CANONICAL order (t74 runs order=keyhash), gamma_t = 1/T. The cap disrupts ARITY only; what truncation does to the scores is not modelled. Per-alert m + r* <= n, not a joint rerun. | labels; NOT e-validity | `t74_defended_replay.py` | Sec. V-D (per-alert reading), App. E | `where $m_t+r^{\star}_t>n$ predicts $91$ and $95$` |
| Joint rerun: per-alert pads applied together leave no true detection | `0.55_keyhash_poly: 0 remain; 0.55_keyhash_uniform: 0 remain; 0.62_keyhash_poly: 0 remain; 0.62_keyhash_uniform: 0 remain` | LSPR23 0.55 and 0.62, seed 0, CANONICAL order, both regimes; controller RE-RUN over the padded stream; zero-evidence pads (pool has no firing flow, asserted) | labels; NOT e-validity. Oracle per-alert pads r* from the unperturbed run. | `t75_joint_rerun.py` | Sec. V-D, Table I caption, App. E | `leaves no true detection standing` |
| Joint rerun: sequential oracle attacker's campaign cost, horizon-aware | `0.55: 5,650 vs per-alert 627,495; 0.62: 9,992 vs per-alert 977,567` | LSPR23 0.55 and 0.62, seed 0, CANONICAL order, gamma_t = 1/T; GREEDY sequential oracle sized on the attacked trajectory's live level -- one strategy's cost, an upper bound on the campaign minimum, not a global minimum. Two windows, one seed. | labels; NOT e-validity. Attacker knows which of its own episodes would fire and the live level (the same oracle knowledge r* assumes). | `t75_joint_rerun.py` | Sec. V-D, Table I joint column, App. E | `$5{,}650$ flows in total` |
| Joint rerun: volume cap against the sequential attacker at cap 100 | `0.55: 96 fire = 44 structural + 52 cascade; 0.62: 79 fire = 33 structural + 46 cascade` | LSPR23 0.55 and 0.62, seed 0, CANONICAL order, gamma_t = 1/T; sequential attacker pads only if m + r <= cap, else the alert fires and raises R. Sampled cap grid; nothing between grid points. | labels; NOT e-validity; zero-evidence pads | `t75_joint_rerun.py` | Sec. V-D, App. E | `$96$ of the $105$ horizon-aware primary-window alerts still fire` |
| State-free multiplier c=3 silences both horizon-aware arms jointly | `0.55: 0 remain at c=3, c_crit=2.134=rho=2.134; 0.62: 0 remain at c=3, c_crit=2.485=rho=2.485` | LSPR23 0.55 and 0.62, seed 0, CANONICAL order, gamma_t = 1/T, EVERY malicious episode multiplied; c_crit <= rho is exact here because a malicious episode attains the ceiling and no false discovery survives. Two windows; not a general theorem. | labels; attacker knows only its own arity m | `t75_joint_rerun.py` | Sec. V-C, App. E | `multiplying \emph{every} own episode by $c=3$ silences it at both windows` |
| AIT host-conditioned detections suppressed under causal context recomputation | `9 of 9 over ['shaw', 'wilson']` | AIT, TWO organisations only, FIRST-FLOW order, host-conditioned detector | labels; the six causal host features and the accumulation model implemented | `t54_ait_suppression.py` | Table I, Sec. V-E | `suppresses all nine` |
